describe('TESTANDO UMA COISA', () => {
    it('should return one', () => {
        const number = 1;

        expect(number).toBe(1);
    });
});

describe('TESTANDO OUTRA COISA', () => {
    test('should return Leonardo', () => {
        const nome = 'Leonardo';

        expect(nome).toBe('Leonardo');
    });
});
